import { HttpErrorInterceptor } from './http-error.interceptor';

describe('HttpError.Interceptor.Ts', () => {
  it('should create an instance', () => {
    expect(new HttpErrorInterceptor()).toBeTruthy();
  });
});
